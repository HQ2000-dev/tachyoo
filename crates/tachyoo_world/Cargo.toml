[package]
name = "tachyoo_world"
version = "0.1.0"
edition = "2024"

[features]
default=["dev"]
dev=[]

[dependencies]
# TODO: determine features
bevy_ecs= { workspace=true, default-features=false, features=["multi_threaded"] }